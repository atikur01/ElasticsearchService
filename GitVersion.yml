next-version: 1.0.0
assembly-versioning-scheme: MajorMinorPatch
tag-prefix: '[v|V]'
branches:
  main:
    regex: '^main$'
    tag: ''
    increment: Patch
  release:
    regex: '^release[/-]'
    tag: ''
    increment: Minor
